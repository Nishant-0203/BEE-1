<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Feedback</title>
    <!-- Tailwind CSS via CDN -->
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-white text-gray-900 min-h-screen">
    <div class="container mx-auto px-4 py-8 max-w-4xl">
        <h1 class="text-3xl font-bold mb-8 border-b pb-4">Feedback Form</h1>
        
        <div class="bg-white shadow-md rounded-lg p-6 mb-10 border border-gray-200">
            <form action="/api/feedback" method="POST" class="space-y-6">
                <div>
                    <label for="name" class="block text-sm font-medium mb-2">Name:</label>
                    <input type="text" id="name" name="name" required 
                        class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-gray-500 transition">
                </div>
                <div>
                    <label for="email" class="block text-sm font-medium mb-2">Email:</label>
                    <input type="email" id="email" name="email" required 
                        class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-gray-500 transition">
                </div>
                <div>
                    <label for="message" class="block text-sm font-medium mb-2">Message:</label>
                    <textarea id="message" name="message" rows="4" required 
                        class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-gray-500 transition"></textarea>
                </div>
                <button type="submit" 
                    class="px-6 py-3 bg-black text-white rounded-md hover:bg-gray-800 focus:outline-none focus:ring-2 focus:ring-gray-500 transition">
                    Submit Feedback
                </button>
            </form>
        </div>
        
        <h2 class="text-2xl font-bold mb-6 border-b pb-3">Previous Feedback</h2>
        <div class="space-y-6">
            <% if (feedbacks && feedbacks.length > 0) { %>
                <% feedbacks.forEach(feedback => { %>
                    <div class="bg-white shadow-sm border border-gray-200 rounded-lg p-6 transition hover:shadow-md">
                        <h3 class="text-xl font-semibold mb-2"><%= feedback.name %></h3>
                        <p class="mb-2"><span class="font-medium">Email:</span> <%= feedback.email %></p>
                        <p class="mb-3"><span class="font-medium">Message:</span> <%= feedback.message || feedback.feedback %></p>
                        <p class="text-sm text-gray-600"><span class="font-medium">Date:</span> <%= new Date(feedback.date || feedback.createdAt).toLocaleString() %></p>
                    </div>
                <% }); %>
            <% } else { %>
                <div class="bg-gray-50 border border-gray-200 rounded-lg p-8 text-center">
                    <p class="text-gray-500">No feedback submitted yet.</p>
                </div>
            <% } %>
        </div>
    </div>
</body>
</html>